using System.Windows.Forms;

namespace TeamManager.Views.Loader
{
    /// <summary>
    /// Nothing more than a window to display the loader.
    /// </summary>
    public partial class LoaderWindow : Form
    {
        /// <summary>
        /// The constructor of the loader to call the <see cref="InitializeComponent"/>.
        /// </summary>
        public LoaderWindow()
        {
            InitializeComponent();
        }
    }
}
